#include <stdio.h>
/**
 * main - the program starts here
 * Return: 0(compiled successfully)
 */

int main(void)
{
	for (char c = 'z'; c >= 'a'; c--)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}
