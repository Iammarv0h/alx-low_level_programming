#include <stdio.h>
/**
 * main- This is the starting point
 * Return: 0(compiled successful)
 */
int main(void)
{
	for (int i = 0; i <= 9; i++)
	{
		putchar(i + '0');
	}
	putchar('\n');
	return (0);
}
